// import { render, cleanup, screen } from "@testing-library/react";
// import renderer from "react-test-renderer";
// import Route from "../dashboard/route/RoutesReport";

// afterEach(() => {
//   cleanup();
// });

// test("should render todo component", () => {
//   render(<Route />);
//   const routeElement = screen.getByTestId("route-1");
//   expect(routeElement).toBeInTheDocument();
//   expect(routeElement).toHaveTextContent("All Routes Report");
//   expect(routeElement).toContainHTML("<DashboardLayout>");
// });

// test("matches snapshot", () => {
//   const tree = renderer.create(<Route />).toJSON();
//   expect(tree).toMatchSnapshot();
// });
